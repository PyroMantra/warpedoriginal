<!DOCTYPE html>
<html>
<head>
  <title>RPG Admin Panel</title>
  <style>
    body { font-family: sans-serif; padding: 2rem; }
    ul { list-style-type: none; padding: 0; }
    li { margin-bottom: 10px; }
    .button {
      display: inline-block;
      padding: 8px 16px;
      background: #333;
      color: white;
      text-decoration: none;
      border-radius: 6px;
    }
    .button:hover {
      background: #555;
    }
  </style>
</head>
<body>
  <h1>\1{{ current_user_name }}</h1>

  <h2>Game Database</h2>
  <ul>
    {% for sheet in sheets %}
      <li><a class="button" href="{{ url_for('view_sheet', sheet=sheet) }}">{{ sheet }}</a></li>
    {% endfor %}
  </ul>

  {% if show_potion_button %}
    <hr>
    <h2>Tools</h2>
    <a class="button" href="{{ url_for('potion_generator') }}">🧪 Open Potion Generator</a>
  {% endif %}

  <br><br>
  <a href="{{ url_for('logout') }}">Log out</a>
</body>
</html>